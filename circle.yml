machine:
  node:
    version: 6
checkout:
  post:
    - git submodule sync --recursive
    - git submodule update --recursive --init
dependencies:
  cache_directories:
    - node_modules
    - storage
    - $HOME/.cache # for Bower cache
